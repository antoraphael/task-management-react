const ProjectDetailsPage: React.FC = () => {
  // read :id, fetch project details + /projects/:id/completion + tasks by project
  return (
    <div>
      <h1 className="text-xl font-semibold mb-4">Project Details</h1>
    </div>
  );
};
export default ProjectDetailsPage;
