const ProjectsListPage: React.FC = () => {
  // const { data: projects } = useGetProjectsQuery();
  return (
    <div>
      <h1 className="text-xl font-semibold mb-4">Projects</h1>
      {/* ProjectCard list + ProjectFormModal */}
    </div>
  );
};
export default ProjectsListPage;
